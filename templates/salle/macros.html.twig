{% macro random_color_class() %}
    {% set colors = ['text-primary', 'text-secondary', 'text-success', 'text-danger', 'text-warning', 'text-info'] %}
    {{ colors[random(0, colors|length - 1)] }}
{% endmacro %}